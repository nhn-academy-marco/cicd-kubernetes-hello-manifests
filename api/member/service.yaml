apiVersion: v1
kind: Service
metadata:
  name: member-service
  namespace: hello
spec:
  selector:
    app: member-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP